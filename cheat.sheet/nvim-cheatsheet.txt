══════════════════════════════════════════════════════════════════════
                         NEOVIM MASTER CHEAT SHEET
                      (ergonomic · 10-finger · pro setup)
══════════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHILOSOPHY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Stay in Normal mode as much as possible
  Insert mode = only for typing NEW text, nothing else
  Use motions + operators instead of manual edits
  Use text objects instead of multiple motions
  Use . to repeat instead of retyping
  Use macros for repetitive multi-step edits
  Avoid arrow keys — use h/j/k/l and motions
  Think in: operator + motion, operator + text-object
  Think in: verb + noun  (d=delete, w=word → dw)
  Stop thinking "how do I move there" → think "what do I do to what object"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MODES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Normal       navigation & commands (default — live here)
  Insert       typing new text only
  Visual       selecting text
  Command      :commands
  jk           → Normal mode from Insert/Visual  [REMAPPED]
  Esc          → Normal mode from any mode

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. NORMAL MODE — MOVEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BASIC
  h j k l          ← ↓ ↑ →
  w / W            next word / WORD (WORD ignores punctuation)
  b / B            previous word / WORD
  e / E            end of word / WORD
  gg               top of file
  G                bottom of file
  {number}G        go to line number  (e.g. 42G)
  %                jump to matching bracket/paren/brace

LINE NAVIGATION
  Ctrl-a           first non-blank character  [REMAPPED from ^]
  Ctrl-e           end of line                [REMAPPED from $]
  0                absolute start of line (including whitespace)
  ^                native: first non-blank character
  $                native: end of line

CHARACTER JUMPS  (extremely powerful, learn these)
  f{char}          jump forward to char on line (cursor lands on char)
  F{char}          jump backward to char on line
  t{char}          jump forward, stop before char
  T{char}          jump backward, stop before char
  ;                repeat last f/F/t/T forward
  ,                repeat last f/F/t/T backward

  EXAMPLE: f( → ; → ; → lands on each ( across the line
  COMBINE:  df(   delete from cursor up to and including (
            dt(   delete up to but not including (
            cf"   change from cursor to next "

COUNTS (combine with any motion)
  5j               move 5 lines down
  3w               jump 3 words forward
  2b               jump 2 words backward

FILE NAVIGATION
  Ctrl-d           half page down (cursor stays centered)  [REMAPPED]
  Ctrl-u           half page up   (cursor stays centered)  [REMAPPED]
  Ctrl-f           full page down
  Ctrl-b           full page up
  Ctrl-e           scroll down 1 line  (conflicts — see LINE NAVIGATION)
  Ctrl-y           scroll up 1 line
  zz               center cursor on screen
  zt               cursor to top of screen
  zb               cursor to bottom of screen

BLOCK / PARAGRAPH JUMPS
  }                jump to next empty line (next paragraph/block)
  {                jump to previous empty line
  ]]               next function or block
  [[               previous function or block

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. JUMP LIST — navigate your history
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Ctrl-o           jump back in history  (works across files!)
  Ctrl-i           jump forward in history
  '.               jump to last edit position
  gd               go to definition of word under cursor
  :jumps           view full jump list

  HOW IT WORKS: every big jump (/, gg, G, %, gd, file switch) is recorded
  PRO WORKFLOW: jump to check something → edit → Ctrl-o to jump back

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. OPERATORS (verbs) — combine with motions or text objects
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  d                delete (cut)
  c                change (delete + enter insert mode)
  y                yank (copy)
  >                indent right
  <                indent left
  =                auto-indent
  g~               toggle case
  gu               lowercase
  gU               uppercase

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. TEXT OBJECTS (nouns) — the most powerful vim concept
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  i = inner (excludes surrounding chars)
  a = around/a (includes surrounding chars)

  iw / aw          inner word / a word (with space)
  is / as          inner sentence / a sentence
  ip / ap          inner paragraph / a paragraph
  i" / a"          inside/around double quotes
  i' / a'          inside/around single quotes
  i` / a`          inside/around backticks
  i( / a(          inside/around parentheses
  i[ / a[          inside/around brackets
  i{ / a{          inside/around curly braces
  i< / a<          inside/around angle brackets
  it / at          inside/around HTML/XML tag

  KEY INSIGHT: cursor can be ANYWHERE on the line — vim finds the pair automatically

EXAMPLES (operator + text object)
  ciw              change inner word (cursor anywhere on word)
  daw              delete a word including trailing space
  ci"              change text inside quotes
  ca(              change everything including the parentheses
  yi{              yank everything inside curly braces
  =ip              auto-indent inner paragraph
  vi"              visually select inside quotes
  di'              delete inside single quotes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. COMMON SHORTCUTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  dd               delete whole line
  cc               change whole line
  yy               yank whole line
  D                delete from cursor to end of line
  C                change from cursor to end of line
  d Ctrl-a         delete to start of line  [REMAPPED]
  d Ctrl-e         delete to end of line    [REMAPPED]
  Y                yank whole line (same as yy)
  x                delete character under cursor
  X                delete character before cursor
  r{char}          replace single character (no insert mode!)
  s                delete char and enter insert mode
  S                delete line and enter insert mode
  u                undo
  Ctrl-r           redo
  .                repeat last change   ← use this constantly
  *                search word under cursor (forward)
  #                search word under cursor (backward)
  Ctrl-a           increment number under cursor  (native)
  Ctrl-x           decrement number under cursor  (native)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. ENTERING INSERT MODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  i                insert before cursor
  a                insert after cursor
  I                insert at start of line
  A                insert at end of line
  o                open new line below and insert
  O                open new line above and insert
  gi               re-enter insert at last insert position

  PRO TIP: prefer A, I, o, O over navigating then pressing i

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. INSERT MODE (minimal — get in, type, get out)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  jk               exit to Normal mode  [REMAPPED]
  Ctrl-a           move to start of line  [REMAPPED]
  Ctrl-e           move to end of line  [REMAPPED]
  Ctrl-o           execute one Normal command then return to Insert
  Ctrl-w           delete word before cursor
  Ctrl-u           delete to start of line
  Ctrl-n / Ctrl-p  next / previous completion suggestion
  Ctrl-r "         paste from default register
  Ctrl-r +         paste from system clipboard

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. COPY / PASTE (registers)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  yy / Y           yank line
  yw               yank word
  yiw              yank inner word
  y$               yank to end of line
  p                paste after cursor / below line
  P                paste before cursor / above line
  gp               paste after, cursor moves past pasted text
  "{reg}y          yank into named register  (e.g. "ay)
  "{reg}p          paste from named register (e.g. "ap)
  "Ay              APPEND to register a (capital = append)
  "+y              yank to system clipboard
  "+p              paste from system clipboard
  "0p              paste last YANK (not affected by deletes)
  "_d              delete to black hole (nothing saved)
  :reg             view all registers
  :reg a b         view specific registers

REGISTER REFERENCE
  ""               default (last yank or delete)
  "0               last yank only
  "1-"9            history of last 9 deletes
  "-               last small delete (less than 1 line)
  "+               system clipboard
  "*               primary selection
  "_               black hole
  ".               last inserted text
  "%               current filename
  "/               last search pattern

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. VISUAL MODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  v                character-wise visual
  V                line-wise visual
  Ctrl-v           block visual (column editing)
  jk               exit visual mode  [REMAPPED]
  o                move to other end of selection
  gv               reselect last visual selection

  THEN apply any operator: d, c, y, >, <, =, g~, r{char}

BLOCK EDITING
  Ctrl-v + j + I   insert at start of multiple lines
  Ctrl-v + j + A   append at end of multiple lines
  Ctrl-v + j + r   replace char across multiple lines

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. SEARCHING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  /pattern         search forward
  ?pattern         search backward
  n                next match
  N                previous match
  *                search word under cursor (forward)
  #                search word under cursor (backward)
  leader+n         clear search highlights  [REMAPPED]

  ignorecase + smartcase:
    lowercase  →  case-insensitive
    Uppercase  →  case-sensitive

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. COMMAND MODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILES
  :w               save
  :q               quit
  :wq / :x         save and quit
  :q!              quit without saving
  :e {file}        open file
  :saveas {file}   save as
  :r {file}        read file into current buffer

SEARCH & REPLACE
  :s/old/new/      replace first on line
  :s/old/new/g     replace all on line
  :%s/old/new/g    replace entire file
  :%s/old/new/gc   confirm each replace
  :%s/\v...        very magic mode (standard regex syntax)
  :g/pattern/d     delete all matching lines
  :sort            sort lines
  Ctrl-r Ctrl-w    paste word under cursor into command line

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. WINDOWS & TABS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPLITS
  leader+-         horizontal split  [REMAPPED]
  leader+|         vertical split    [REMAPPED]
  leader+h/j/k/l   navigate between splits  [REMAPPED]
  leader+q         close current split/window  [REMAPPED]
  Ctrl-w =         equalize split sizes
  Ctrl-w +/-       resize split height
  Ctrl-w >/<       resize split width

TABS
  leader+t         new tab       [REMAPPED]
  leader+c         close tab     [REMAPPED]
  gt               next tab
  gT               previous tab
  {n}gt            go to tab n

BUFFERS
  :bn              next buffer
  :bp              previous buffer
  :bd              delete (close) buffer
  :ls              list all buffers

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. MACROS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  q{reg}           start recording macro (e.g. qa)
  q                stop recording
  @{reg}           play macro (e.g. @a)
  @@               repeat last macro
  5@a              run macro 5 times

  TIPS:
  - Plan the full action before recording
  - End macro on a position ready for the next repeat
  - Combine with text objects for reliable edits
  - Use . inside macros to repeat sub-actions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. MARKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  m{a-z}           set local mark (e.g. ma)
  '{mark}          jump to mark line
  `{mark}          jump to exact mark position
  ''               jump back to last jump position
  Ctrl-o / Ctrl-i  walk jump history back / forward

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. LEADER KEY MAPPINGS  (Leader = Space)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILE OPERATIONS
  leader+w         save
  leader+q         quit / close split
  leader+x         save and quit
  leader+e         :e  (open file)
  leader+leader    : (command mode)

TABS & SPLITS
  leader+t         new tab
  leader+c         close tab
  leader+|         vertical split
  leader+-         horizontal split
  leader+h/j/k/l   navigate splits

SEARCH
  leader+n         clear search highlight

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
16. PRO PATTERNS (daily muscle memory goals)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ciw              change a word anywhere (no need to go to start)
  ct{char}         change up to a character on the line
  df{char}         delete including a character on the line
  dt{char}         delete up to but not including a character
  yiw → p          copy word, move, paste
  "0p              safe paste — last yank, unaffected by deletes
  "_diw            delete without overwriting your yank register
  gg=G             auto-indent entire file
  :%s/\s\+$//      remove trailing whitespace
  vi{ then =       select block and auto-indent
  . after any op   repeat the last change at new cursor position
  *Ncgn            search word, change match, . to repeat on next
  Ctrl-o           jump back after any navigation — use constantly
  f{char} + ;      fly along a line hitting each occurrence
  v$r=             fill to end of line with = (or any char)
  Ctrl-v j r{c}    replace same column across multiple lines

══════════════════════════════════════════════════════════════════════
                              END OF GUIDE
══════════════════════════════════════════════════════════════════════
